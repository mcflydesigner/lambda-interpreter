-| "std.nk";

/\ n: Int. {
    | ((n > 1) and (n < 5)): (
        let x = (n + 5) * 10 in x*x;
         readReal (n); // returns unit
    )
    | ((n >= 10) or (n < 0)): (n * 3) + 5 // returns int
    |:  n * (n+1); //returns int
};