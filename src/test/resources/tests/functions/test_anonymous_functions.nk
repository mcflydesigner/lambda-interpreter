let first_class_function = /\ x: String. {
    /\ y: String. {
       print (x + y);
    };
}
let result_function = first_class_function ("Hello, ");
print (result_function ("World!"));
// Output:
// "Hello, World!"