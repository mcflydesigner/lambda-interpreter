// Incorrect function defining

let concat = func (x: String, y: String) {
    x + y
};

let concat = /\ x: String. y: String. {
    x + y
};

let concat = /\ x: String, y: String {
    x + y
};

let concat = /\ (x: String. y: String) {
    x + y
};

let concat = (/\ x: String. y: String.) {
    x + y
};

concat ("Hello,", "World");